#include <bits/stdc++.h>
using namespace std;
set<string> nhap(string s){
    stringstream ss(s);
    string token;
    set<string> se;
    while(ss >> token){
        se.insert(token);
    }
    return se;
}
int main (){
    int t; cin >> t;
    cin.ignore();
    while(t--){
        string s1, s2;   
    }
}
